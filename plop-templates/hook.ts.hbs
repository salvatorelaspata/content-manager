import { useState, useEffect } from 'react';

const {{camelCase name}} = () => {
    const [{{camelCase name}}, set{{pascalCase name}}] = useState<any>(null)
    useEffect(() => {

    },[])

    return [{{camelCase name}}, set{{pascalCase name}}]
};

export default {{camelCase name}};